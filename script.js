const shareButton=document.querySelector('#share');if(shareButton){shareButton.addEventListener('click',async()=>{try{if(navigator.share){await navigator.share({title:document.title,text:'Itinerario de viaje por los Dolomitas',url:location.href});return}await navigator.clipboard.writeText(location.href);const old=shareButton.textContent;shareButton.textContent='Enlace copiado';setTimeout(()=>shareButton.textContent=old,1600)}catch{}})}